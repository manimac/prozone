import { SetBooleanPipe } from './set-boolean.pipe';

describe('SetBooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new SetBooleanPipe();
    expect(pipe).toBeTruthy();
  });
});
